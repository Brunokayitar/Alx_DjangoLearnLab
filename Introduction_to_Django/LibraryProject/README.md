# LibraryProject

This is a basic Django project created as part of the ALX Django setup task.

## Project Structure

- settings.py → Project configuration
- urls.py → URL routing
- manage.py → Django command-line utility

## Running the Project

Start the development server:

python manage.py runserver
